@model IEnumerable<DM2.Models.ArticulosTag>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tags.NombreTag)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdArticle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tags.NombreTag)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdArticle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdArtTag }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdArtTag }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdArtTag })
        </td>
    </tr>
}

</table>
